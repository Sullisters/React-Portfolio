import React from 'react';

function Hello() {
    const message = 'test message';

    return (
        <div className='container'>
            <h2>{message}</h2>
        </div>
    );
}

export default Hello;